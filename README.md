# Nadi42

Nadi42 contains the frontend and backend applications:

- `frontend/` - Vite frontend
- `backend/` - Express and MongoDB backend

## Run locally

Install dependencies in each application directory, then run their package scripts.